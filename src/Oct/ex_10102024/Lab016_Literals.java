package Oct.ex_10102024;

public class Lab016_Literals {
    public static void main(String[] args) {

        //Boolean literal -- true and false
        boolean is_boy = true;
        boolean is_girl = false;

        //Int literal -- Decimal system
        int age = 21;
        int abc = -1;
        // -1 is stored by using the concept of 1's compliment

        //Binary literal
         int bn = 0b001;

        // Octal base? -> 8
        int octal = 0101; // 0101 = (0 × 8³) + (1 × 8²) + (0 × 8¹) + (1 × 8⁰) = 65

        // Hexadecimal Literals
        int hex = 0Xface; //base 16 color combination are in hex - #28a745


    }
}
